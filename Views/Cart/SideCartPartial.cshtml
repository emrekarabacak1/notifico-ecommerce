@model IEnumerable<Notifico.Models.CartItem>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info mt-3">Sepetiniz boş.</div>
}
else
{
    <ul class="list-group mb-2">
        @foreach (var item in Model)
        {
            <li class="list-group-item d-flex justify-content-between align-items-center gap-2" data-id="@item.Id">
                <div class="d-flex align-items-center gap-2">
                    <img src="@(string.IsNullOrEmpty(item.Product?.ImageUrl) ? "/images/no-image.png" : item.Product.ImageUrl)"
                         alt="@item.Product?.Name"
                         style="width:40px;height:40px;object-fit:cover;border-radius:8px;" />
                    <div>
                        <div class="fw-semibold">@item.Product?.Name</div>
                        <div class="text-muted small">@item.Product?.Price.ToString("C")</div>
                    </div>
                </div>
                <div>
                    <button class="btn btn-sm btn-outline-secondary btn-decrease" data-id="@item.Id" @(item.Quantity == 1 ? "disabled" : "")>-</button>
                    <span class="mx-1" id="qty_@item.Id">@item.Quantity</span>
                    <button class="btn btn-sm btn-outline-secondary btn-increase" data-id="@item.Id" @(item.Product != null && item.Quantity == item.Product.Stock ? "disabled" : "")>+</button>
                    <button class="btn btn-sm btn-danger btn-remove ms-2" data-id="@item.Id"><i class="bi bi-trash"></i></button>
                </div>
            </li>
        }
    </ul>
    <div class="text-end">
        <strong>Toplam: <span id="sideCartTotal">@Model.Sum(x => (x.Product?.Price ?? 0) * x.Quantity).ToString("C")</span></strong>
    </div>
    <div class="mt-3 d-flex justify-content-between">
        <button class="btn btn-outline-danger btn-sm" id="sideCartClearBtn">Sepeti Boşalt</button>
        <a href="/Cart/MyCart" class="btn btn-primary btn-sm">Sepete Git</a>
    </div>
}



