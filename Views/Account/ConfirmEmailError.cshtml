@{
    ViewData["Title"] = "Onay Hatası";
}
<div class="alert alert-danger mt-5 text-center">
    <h2><i class="bi bi-x-circle-fill text-danger"></i> E-posta onayı başarısız oldu!</h2>
    <p>Link süresi dolmuş veya hatalı olabilir.</p>
    <a href="@Url.Action("Register", "Account")" class="btn btn-secondary mt-3">Kayıt Ol</a>
</div>
